<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme used before Compose takes over. Keeps splash background consistent. -->
    <style name="Theme.Hikiyose" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/seed</item>
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>
